import React from 'react';
// import {ReactComponent as Loader} from '../

const Loading = (props) => {
    return (
    <div className='Loading'>
        <h3>loading...</h3>
    </div>
    )
}

export default Loading;